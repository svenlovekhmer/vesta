<div class="modal-header">
  <% if client.new_record? %>
    <h5 class="modal-title">Ajouter un client</h5>
  <% else %>
    <h5 class="modal-title">Modifier le client</h5>
  <% end %>
  <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Fermer"></button>
</div>
<div class="modal-body">
  <%= render "form", client: client %>
</div>
<div class="modal-footer">
  <button type="button" class="text-danger cancel-btn border-0 bg-transparent text-black" data-bs-dismiss="modal">
    <i class="fa-solid fa-angle-left d-inline-block me-2"></i> Annuler
  </button>
  <button type="submit" form="client_form" class="btn btn-danger vesta-btn text-white rounded-pill">
    <i class="fa-solid fa-save d-inline-block me-2"></i> Valider
  </button>
</div>
