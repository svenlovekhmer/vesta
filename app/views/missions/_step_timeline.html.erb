<div class="card">
  <h6><i class="fa-solid fa-list-check text-secondary"></i> Progression de la mission</h6>

  <div class="step-timeline mt-4">
    <% mission.steps.each_with_index do |step, index| %>
      <%= render "missions/step_item",
                 step: step,
                 is_last: index == mission.steps.length - 1,
                 step_statuses: local_assigns[:step_statuses] || StepStatus.all %>
    <% end %>
  </div>
</div>
